enum Key {
    ArrowDown = '{downarrow}',
    ArrowLeft = '{leftarrow}',
    ArrowRight = '{rightarrow}',
    ArrowUp = '{uparrow}',
    Control = '{ctrl}',
    Enter = '{enter}',
    Escape = '{esc}',
    Shift = '{shift}'
}

export default Key;