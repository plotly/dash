enum Key {
    ArrowDown = '{downarrow}',
    ArrowLeft = '{leftarrow}',
    ArrowRight = '{rightarrow}',
    ArrowUp = '{uparrow}',
    Enter = '{enter}',
    Escape = '{esc}',
    Shift = '{shift}'
}

export default Key;